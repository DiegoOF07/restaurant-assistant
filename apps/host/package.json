{
  "name": "@restaurant/host",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "echo 'lint placeholder for host'"
  },
  "dependencies": {
    "@restaurant/conversation": "workspace:*",
    "@restaurant/llm-provider": "workspace:*",
    "@restaurant/mcp-client": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.3.0",
    "typescript": "^7.0.2",
    "vitest": "^5.0.0"
  }
}