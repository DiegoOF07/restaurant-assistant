# pnpm-workspace.yaml
packages:
  - "apps/*"
  - "packages/*"

# esbuild descarga su binario nativo en un script de instalación. Vite lo necesita para
# compilar el frontend, así que se permite explícitamente.
allowBuilds:
  esbuild: true
